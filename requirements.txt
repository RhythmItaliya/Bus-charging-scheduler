# Bus Charging Scheduler — Python dependencies
# Pinned to the exact versions used in development.
# PyArrow is bundled with Streamlit — no need to list it separately.

streamlit==1.58.0   # Web UI framework — handles scenario picker, tabs, sliders
pandas==2.3.3       # DataFrames for the three table views in the UI
rich>=13.0.0        # Beautiful terminal output — panels, tables, colours for CLI demo
                    # (already bundled with Streamlit; listed here for clarity)
